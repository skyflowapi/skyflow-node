/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Skyflow from "../index";

/**
 * Column group-level rule details.
 */
export interface V1ColumnGroupRuleParams {
    /** ID of the vault that contains the column group(s). */
    vaultID?: string;
    /** Column group(s) that the rule applies to. */
    columnGroups?: string[];
    action?: Skyflow.V1Action;
    effect?: Skyflow.V1Effect;
    /** SQL expression that applies a filter on all rows of a table. */
    rowFilter?: string;
    /** Redaction type applied to values in the specified columns group(s). */
    redaction?: string;
    actions?: Skyflow.V1Action[];
}
