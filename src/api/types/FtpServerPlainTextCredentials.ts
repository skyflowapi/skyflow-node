/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * Plain text credentials for the file transfer protocol.
 */
export interface FtpServerPlainTextCredentials {
    /** Hostname of the server. */
    hostname?: string;
    /** Port to access the server. */
    port?: string;
    /** Username to access the server. */
    username?: string;
    /** Password to access the server. */
    password?: string;
    /** ID of the SSH key to access the server. */
    sshKeyID?: string;
}
