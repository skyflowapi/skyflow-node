/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Skyflow from "../index";

/**
 * Mapping details for a field in the source data.
 */
export interface FieldMappingSourceField {
    /** Name of a column in a CSV, TSV, PSV or PARQUET source file. */
    columnName?: string;
    /** Name of a property in a JSON source file. */
    jsonFieldName?: string;
    /** Specification for custom data formats. */
    regularExpression?: string;
    /** If inserting or migrating existing tokens, this is the name of the column in the source file that contains the existing tokens. Values in this column are inserted as tokens for values in `columnName`. If the target column is configured for tokenization but this field isn't specified or tokens aren't present for a given value, Skyflow generates tokens according to the target column's configuration. Only valid for `CSV`, `TSV` and `PSV` data formats. */
    tokenColumnName?: string;
    fixedWidthField?: Skyflow.V1FixedWidthField;
}
