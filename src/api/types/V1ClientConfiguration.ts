/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * Client-side configuration for a service account.
 */
export interface V1ClientConfiguration {
    /** When `true`, all JWT assertions for this service account much contain a `ctx` claim. */
    enforceContextID?: boolean;
    /** When `true`, all data tokens sent to the vault using this service account must be signed with the associated private key. */
    enforceSignedDataTokens?: boolean;
}
