/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Skyflow from "../index";

/**
 * Rule details.
 */
export interface V1Rule {
    /** ID of the rule. */
    ID?: string;
    /** Name of the rule. Must be unique within a policy. */
    name: string;
    effect?: Skyflow.V1Effect;
    /** Actions the rule is scoped to. */
    actions?: string[];
    /** Resources the rule applies to. */
    resources?: string[];
    resourceType?: Skyflow.RuleResourceType;
    dlpFormat?: Skyflow.V1DlpFormat;
    /** A Common Expression Language that applies a conditional filter. */
    condition?: string;
    /** SQL expression that applies a filter on all rows of a table. */
    rowFilter?: string;
    /** The rule expressed as a formatted string. */
    ruleExpression?: string;
    /** The redaction expression. */
    redaction?: string;
}
