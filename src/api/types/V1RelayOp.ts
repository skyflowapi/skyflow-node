/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Skyflow from "../index";

export interface V1RelayOp {
    action?: Skyflow.RelayOpActions;
    /** Name (path) of the field that the action acts on. JSON uses '.' as a separator. */
    fieldName?: string;
    /** Name of the table to store the token in. Required only if `action` is `TOKENIZATION`. */
    table?: string;
    /** Name of the column to store the token in. Required only if `action` is `TOKENIZATION`. */
    column?: string;
    /** Method to select data within a field. */
    dataSelector?: string;
    /** Regular expression to select data within a field. Required only if `dataSelector` is `REGEX`. */
    dataSelectorRegex?: string;
    /** Directive to change format of the data. */
    transformFormat?: string;
    /** Type of encryption. Required only if `action` is `ENCRYPTION`. <br> Accepted values:<ul><li>AES_IV_FRONT_APPEND</li></ul> */
    encryptionType?: string;
    redaction?: Skyflow.RedactionEnumRedaction;
    /** Regex to match the given field value. */
    sourceRegex?: string;
    /** Regex to create the transformed value. */
    transformedRegex?: string;
}
