/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * Account related data.
 */
export interface V1PanData {
    /** The real Primary Account Number (PAN). Mandatory - It is required except when panReferenceId is provided. */
    pan?: string;
    /** The expiry date of the real PAN. The format is 'YYMM'. Mandatory - It is required except when panReferenceId is provided. */
    pan_exp_date?: string;
    /** The security code embedded on the back of the physical credit or debit card. Optional. */
    cvv?: string;
    /** The unique reference ID (GUID format) of the real PAN. Mandatory - It is required except when pan and panExpDate are present. */
    pan_reference_id?: string;
    /** The PAN sequence number (PSN). Optional. */
    pan_sequence_number?: string;
}
