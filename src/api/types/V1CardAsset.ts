/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * CardAsset details.
 */
export interface V1CardAsset {
    /** Unique identifier (GUID format) of the asset. Mandatory - for VTS, the maximum length is 32; for MDES, the maximum length is 64. */
    guid?: string;
    /** The type of the asset. It has pre-defined values. Mandatory. */
    asset_type?: string;
    /** The mime type of the asset. It has pre-defined values. Optional. */
    mime_type?: string;
    /** The width of the image in pixels. Set to zero for non-image MIME-types. Optional. */
    pixel_height?: string;
    /** The height of the image in pixels. Set to zero for non-image MIME-types. Optional. */
    pixel_width?: string;
}
