/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * Details of the Card.
 */
export interface V1CardData {
    /** The unique reference ID (GUID format) of the real PAN. It is required except when PAN is provided. Optional. */
    pan_reference_id?: string;
    /** Globally unique reference of a cardholder's PAN. It is commonly abbreviated as PAR. Optional. */
    payment_account_reference?: string;
    /** The last four digits of the real PAN. Optional. */
    card_suffix?: string;
    /** The expiry date of the real PAN. The format is 'YYMM'. Optional. */
    pan_exp_date?: string;
    /** The embossed name on the physical card. Optional. */
    card_holder_embossed_name?: string;
    /** A 2 character country code. The format is based on ISO 3166-1 alpha-2. Optional. */
    card_country_code?: string;
    /** The name of the card. Optional. */
    card_name?: string;
    /** The type of the card product (e.g. Credit, Debit). Optional. */
    card_type?: string;
    /** The brand of the card(e.g. VISA, MASTERCARD). Optional. */
    card_brand?: string;
    /** Card long description. */
    card_long_description?: string;
    /** Card short description. */
    card_short_description?: string;
    /** It indicates if the card is a co-branded card. Optional. */
    co_branded?: boolean;
    /** The card co-brand name. It is required if isCoBranded is set to true. Optional. */
    co_brand_name?: string;
    /** These are indicators (Boolean values) associated with the card. */
    card_type_indicator?: string;
    /** It indicates if the expiration date is printed on the card or not. Optional. */
    exp_data_printed?: boolean;
    /** It indicates if the cvv2 is printed on the card or not. Optional. */
    cvv2_printed?: boolean;
}
