/**
 * This file was auto-generated by Fern from our API Definition.
 */

export interface V1GetWebhookResponse {
    /** Identifier for the webhook. */
    webhookID?: string;
    /** Name of the webhook. */
    name?: string;
    /** Display name of the webhook. */
    displayName?: string;
    /** Description of the webhook. */
    description?: string;
    /** Namespace of the webhook. */
    namespace?: string;
    /** URL that should receive events for the webhook. */
    URL?: string;
    /** List of event types that trigger events for the webhook, in `<resource>.<action>` format. `*` can be used to indicate all action for all resources, or as a substitute for specific a action. For example, `pipelineRun.*` sends a webhook event for every pipeline run action, while `deidentifyFile.completed` only sends a webhook event when the deidentification of a file is done. */
    eventTypes?: string[];
}
