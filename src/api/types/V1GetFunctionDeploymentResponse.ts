/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Skyflow from "../index";

export interface V1GetFunctionDeploymentResponse {
    /** ID of the deployment. */
    ID?: string;
    /** Name of the deployment. */
    name?: string;
    /** ID of the function. */
    functionID?: string;
    /** Version of the function. */
    versionTag?: string;
    /** ID of the environment to deploy the function in. */
    functionEnvironmentID?: string;
    status?: Skyflow.V1ObjectStatus;
    /** List of all function methods in the deployment. */
    methodNames?: string[];
    BasicAudit?: Skyflow.V1BasicAudit;
    /** Error message returned if the function deployment is in a <code>FAILED</code> state. */
    errorMessage?: string;
    /** ID of the region to deploy the function in. */
    regionID?: string;
    /** Name of the function environment. */
    functionEnvironmentName?: string;
    /** Description of the deployment. */
    description?: string;
}
