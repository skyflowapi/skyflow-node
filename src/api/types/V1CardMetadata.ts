/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Skyflow from "../index";

/**
 * Metadata about the card.
 */
export interface V1CardMetadata {
    /** Unique identifier (GUID format) of the terms and conditions. Optional - for VTS, the maximum length is 32; for MDES, the maximum length is 64. */
    terms_and_conditions_id?: string;
    /** The website URL which is used to describe the terms and conditions. Optional. */
    terms_and_conditions_url?: string;
    /** The website URL which is used to present card's art image. Optional. */
    card_art_url?: string;
    /** The background color of the card specified as a CSS style RGB triple (hex format). Optional. */
    card_background_color?: string;
    /** The foreground color of the card specified as a CSS style RGB triple (hex format). Optional. */
    card_foreground_color?: string;
    /** The color of the label of the card specified as a CSS style RGB triple (hex format). Optional. */
    label_color?: string;
    /** The detailed description of the card product to be displayed inside the mobile wallet. Optional. */
    long_description?: string;
    /** The short description of the card product to be used for notification on the mobile device. Optional. */
    short_description?: string;
    issuer_data?: Skyflow.V1IssuerData;
    issuer_app_data?: Skyflow.V1IssuerAppData;
    /** Array of Card Assets. */
    card_assets?: Skyflow.V1CardAsset[];
}
