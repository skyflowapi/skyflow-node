/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Skyflow from "../index";

export interface V1ServiceAccountResponse {
    /** ID of the service account. */
    clientID?: string;
    /** Name of the service account. */
    clientName?: string;
    /** Token issuer URI. */
    tokenURI?: string;
    /** ID of the key linked with the service account. */
    keyID?: string;
    /** Key linked with the service account. */
    privateKey?: string;
    /** Timestamp when the key becomes valid. */
    keyValidAfterTime?: string;
    /** Timestamp the key is valid until. */
    keyValidBeforeTime?: string;
    /** ID of the API key linked with the service account. */
    apiKeyID?: string;
    /** API key linked with the service account. */
    apiKey?: string;
    keyAlgorithm?: Skyflow.V1ServiceAccountKeyAlgorithm;
}
