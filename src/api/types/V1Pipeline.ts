/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Skyflow from "../index";

/**
 * Pipeline specifications.
 */
export interface V1Pipeline {
    /** ID of the pipeline. */
    ID?: string;
    /** Name of the pipeline. */
    name?: string;
    /** Display name of the pipeline. */
    displayName?: string;
    /** Description of the pipeline. */
    description?: string;
    /** Namespace of the pipeline. */
    namespace?: string;
    source?: Skyflow.PipelinesSourceDatastoreOne | undefined;
    destination?: Skyflow.V1Datastore;
    /** Mappings of source data fields to vault tables and columns. */
    dataMappings?: Skyflow.V1DataMapping[];
    /** Custom arguments required by the pipeline. */
    args?: Record<string, string>;
    status?: Skyflow.V1ObjectStatus;
    action?: Skyflow.V1PipelineAction;
    /** Trigger mechanisms supported by the pipeline. */
    runTriggers?: Skyflow.PipelineRunTrigger[];
    /** Column in the vault that stores a primary key. The column must be set as unique in the vault schema. */
    primaryKey?: string;
    reportOptions?: Skyflow.V1ReportOptions;
}
