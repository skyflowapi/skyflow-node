/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Skyflow from "../index";

/**
 * Response fields for a specified pipeline run report.
 */
export interface V1PipelineRun {
    /** ID of the pipeline run. */
    ID?: string;
    /** ID of the pipeline. */
    pipelineID?: string;
    state?: Skyflow.PipelineRunState;
    /** Error message returned from the pipeline if state is <code>FAILED</code>. */
    error?: string;
    /** Date time that the pipeline run started. */
    startedOn?: string;
    /** Date time that the pipeline run completed. */
    completedOn?: string;
    /** URL of the file that lists errors encountered from the parse task during the pipeline run. */
    parserErrorFileURL?: string;
    /** URL of the file that lists errors encountered from the vault task during the pipeline run. */
    vaultErrorFileURL?: string;
    /** URL of the file composed of tokens generated during the pipeline run. */
    tokenFileURL?: string;
    /** URL of the file that lists errors encountered from the vault task during the pipeline run. */
    detectReportFileURL?: string;
}
