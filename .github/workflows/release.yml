name: Public Release

on:
  push:
    tags: '*.*.\d+'
    paths-ignore:
      - "package.json"
      - "package-lock.json"
      - "*.md"

jobs:
  call-common-workflow:
    uses: ./.github/workflows/common-release.yml
    with:
      tag: public
    secrets: inherit
